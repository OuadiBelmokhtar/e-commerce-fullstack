export interface Customer {
    name: string;
    email: string;
    phoneNumber: string;
    address: string;
    username: string;// utile pr identifer le user qui a achete la commande, lors de l'envoi des données au backend.

}